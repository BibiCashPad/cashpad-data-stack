.env
*.log

# macOS
.DS_Store
**/.DS_Store

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Dagster local state — garde dagster.yaml (config), ignore le reste
dagster/dagster_home/*
!dagster/dagster_home/dagster.yaml
dagster/**/__pycache__/
dagster/**/*.pyc

# Claude Code — garde settings.json (project, partagé), ignore le reste
.claude/settings.local.json
.claude/scheduled_tasks.lock
